# Dependencies
node_modules

# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# @vantol/presence signing keys. Never commit the private half; the public half
# is regenerated with it on every machine that lacks the pair, so it is no use
# to anyone either. Verification uses the key served in the page's runtime config.
**/.presence/

# Local env files
.env
.env.*
!.env.example

.superpowers
/superpowers

# Git worktrees (created by using-git-worktrees skill)
.worktrees/
# Vercel
.vercel

# Sibling repositories (each has its own remote)
/apps/zhyjen/

AGENTS.md
CLAUDE.md
GEMINI.md

# Local Better Auth SQLite DB
.data/
