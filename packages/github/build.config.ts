/**
 * Build config for `@nuxt-presence/github`. Same shape as nuxt-presence.
 *
 * `failOnWarn: false` — subpath package.json warnings are informational when
 * Node resolves modules via the top-level `exports` field.
 */
export default {
  failOnWarn: false,
};
